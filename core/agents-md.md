# AGENTS.md — ${stack}

> Este archivo es leído por agentes externos (Codex, Cursor, etc.). Claude Code usa `CLAUDE.md` y `.claude/`.

## Stack

${stack} (${language})

## Estructura del Proyecto

```
${structure}
```

## Convenciones

- Commits: `tipo(scope): descripción`
- Branches: `${branch_pattern}`
- Tickets: `${ticket_format}`

<!-- origen: UDLA_backend_ssh/CLAUDE.md §Claude Code NO lee AGENTS.md (generalizado) -->
